Surface container for grouped content. Use `glass` for panels floating over the live map, `sheet` for the bottom route drawer.

```jsx
<Card variant="glass">Floating route summary</Card>
<Card variant="sheet">Bottom drawer content</Card>
```

Variants: `raised` (default opaque card), `glass` (frosted overlay), `sheet` (drawer, rounded top only), `flat`.
